# Computer_Vision_Assignment_01

Run the full assignment with `py -3 src\main.py`.

Outputs are written under `outputs/q1` through `outputs/q12`.

Note for Q7: the repository did not include separate small/large image pairs, so the code generates reduced versions from the provided originals and then evaluates the manual zoom methods by scaling them back up.
